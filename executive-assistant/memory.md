# Memory

## Preferences & Decisions

- **sheets-digest skill** — reads recent Google Sheets, finds sales/outreach metrics (shows, closes, revenue, discovery calls, strategy sessions), maps them to OS stat categories, and calls `update_os_stats` to write them into the Dashboard and Analytics panel. Does NOT write to Notion. Does NOT dump raw sheet data. Follow SKILL.md exactly.
