# Sheets Digest — 2026-08-28

**Source:** DigiGrowth Sales Performance Tracker (always read). No Cold Calling Metrics file (`[Month Year] DigiGrowth Cold Calling Metrics`) was modified in the last 24h, so no cold calling sheet was read this run.

## Sales Performance Tracker

19 rows with data (Call 1–19), most recent entry 08/20/26 (Austin Treadwell). Sheet's `modifiedTime` (2026-08-27T16:35:51.814Z) advanced since the last sync, but no new call rows were added — underlying data unchanged.

| Metric | All-time | 30D | 7D |
|---|---|---|---|
| discovery_calls (booked) | 19 | 1 | 0 |
| shows | 10 | 1 | 0 |
| closes | 0 | 0 | 0 |
| total_revenue | 0 | 0 | 0 |

Call 19 (08/20/26, Austin Treadwell) is now outside the 7D window (today − 7 = 08/21) but still inside the 30D window relative to today (2026-08-28). 7D metrics dropped from 1 to 0 vs. yesterday's run.

## Updated

`dashboard/backend/sales_stats.json` — `discovery_calls_7d` and `shows_7d` rolled from 1 → 0 as Call 19 aged out of the 7-day window; refreshed `last_sheet_sync` and `sheet_data_last_changed` timestamps and note.

## Cold Calling Metrics

No cold calling sheet opened today. Sales tracker updated only.
